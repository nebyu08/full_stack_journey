let gamePattern=[];
let userPattern=[];
let state="ongoing"


//genrate patter
function generatePattern(){
    let gen= Math.floor(Math.random()*4+1);
    gamePattern.push(gen);
}


//number to pattern
function numberToPattern(currentNum){
    let pattern;
    switch(currentNum){
        case
    }
}
